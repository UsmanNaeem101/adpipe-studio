#!/usr/bin/env bash
# adpipe launcher — always uses the project venv so the anthropic SDK is present.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$here/.venv/bin/python" "$here/pipeline/cli.py" "$@"
